import { SummonerSpellsDto } from './summoner-spells-dto';

describe('SummonerSpellsDto', () => {
  it('should create an instance', () => {
    expect(new SummonerSpellsDto()).toBeTruthy();
  });
});
